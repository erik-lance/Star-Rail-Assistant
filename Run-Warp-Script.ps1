# This simply runs the `Get-Warp-History.ps1` script and copies the URL to the clipboard.
# This is to make the powershell window stay open after the script is done.

# Run the script
powershell -File Get-Warp-History.ps1